<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="{{ styles 'style.css' }}">
    <title>{{ title }}</title>
</head>
<style>
    .topnav {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #2c3e50;
        padding: 10px 20px;
    }
  .topnav a:hover,
  .topnav button:hover {
      background-color: floralwhite;
      color: black;
  }

    /* Style the links inside the navigation bar */
    .topnav a {
        margin-left: 20px;
        margin-right: 20px;
        color: black;
        text-align: center;
        text-decoration: none;
        font-size: 17px;
        background-color: #2c3e50;
    }

    /* Change the color of links on hover */
    .topnav a {
        color: white;
        text-decoration: none;
        font-size: 17px;
        padding: 10px;
        transition: background-color 0.3s;
    }

    .topnav a:hover {
        background-color: floralwhite;
    }

    .topnav a.active {
        background-color: #2196F3;
        color: white;
    }

    .topnav input[type=text] {
        padding: 10px;
        border: 2px solid #2c3e50; /* Midnight color border */
        font-size: 17px;
        border-radius: 5px;
    }

    .topnav button {
        margin-left: 20px;
        margin-right: 20px;
        color: white;
        text-align: center;
        text-decoration: none;
        font-size: 17px;
        background-color: #2c3e50;
        padding: 10px;
        border: none;
        border-radius: 5px;
        transition: background-color 0.3s;
    }

    .topnav button:hover {
        background-color: floralwhite;
        color: black;
    }

    nav ul {
        list-style-type: none;
        margin: 0;
        padding: 0;
        text-align: right;
    }

    nav ul li {
        display: inline-block;
        margin-left: 20px;
    }

    nav ul li a {
        color: white;
        text-decoration: none;
    }

    nav ul li a:hover {
        text-decoration: none;
    }
  .pinteresting {
        font-family: 'Courier New', Courier, monospace; /* Example special font */
        font-size: 17px; /* Adjust font size as needed */
        color: white;
        text-decoration: none;
        transition: background-color 0.3s;
    }

    .pinteresting:hover {
        background-color: floralwhite;
    }
   .account-icon {
    display: flex;
    align-items: center;
}

.account-icon {
    margin-right: 10px; /* Remove margin-right */
    float: left; /* Float the icon to the left */
}

.account-icon:hover > img {
    background-color: floralwhite;
}

</style>

<script>
    document.getElementById('searchButton')?.addEventListener('click', function() {
         
        let searchItem = document.getElementById('search')?.value;
         
    });
</script>

<body>
    <header>
        <div class="topnav">
           <nav>
                <ul>
             {{#if loggedIn}}
           <a class="pinteresting" href="{{ PostView }}">Pinteresting</a>
            <input id="search" type="text" placeholder="Search..">
            <button id="searchButton">Search</button>
           
            <!-- if not logged in, the icon will lead to login, if logged in, icon will show account details-->
              </ul>
            </nav>
            <a href="users/{{userId}}">
                <img src="https://static.vecteezy.com/system/resources/previews/021/079/672/original/user-account-icon-for-your-design-only-free-png.png" alt="Account Icon" style="float:right;width:50px;" />
            </a>
 <nav>
                <ul>
             <li><a href="{{ path 'posts/new' }}">Create Post</a></li>
              <li><a href="{{ path 'logout' }}">Logout</a></li>
             {{else}}
             
                <a class="pinteresting" href="{{ path '' }}">Pinteresting</a>
                  <input id="search" type="text" placeholder="Search..">
                  <button id="searchButton">Search</button>
                    </ul>
            </nav>
                    <a href="{{ path 'login' }}" class="account-icon">
                <img src="https://static.vecteezy.com/system/resources/previews/021/079/672/original/user-account-icon-for-your-design-only-free-png.png" alt="Account Icon" style="width:50px;" />
                    </a>
                     <nav>
                <ul>
                     <li><a href="{{ path 'register' }}">Register</a></li>
                    <li><a href="{{ path 'login' }}">Login</a></li>
                    {{/if}}
                </ul>
            </nav>
        </div>
    </header>
</body>

</html>
